# PostgreSQL connection string
DATABASE_URL=postgresql://usuario:contraseña@host:puerto/basededatos

# JWT secret key (usá una cadena larga y segura)
JWT_SECRET=mi_clave_super_secreta

# Puerto del servidor (opcional)
PORT=3000
